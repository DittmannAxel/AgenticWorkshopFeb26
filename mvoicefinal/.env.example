# --------------------------------------------------------------------
# mvoicefinal - Environment configuration (copy to .env and fill in)
#
# Usage:
#   cp .env.example .env
#   python main_agent.py --use-token-credential
#
# Notes:
# - If you use `--use-token-credential`, you typically do NOT need `AZURE_VOICELIVE_API_KEY`.
# - If you use API key auth, pass `--api-key` or set `AZURE_VOICELIVE_API_KEY`.
# --------------------------------------------------------------------

# ---------------------------
# Azure Voice Live (required)
# ---------------------------
# Example format:
#   https://<resource>.services.ai.azure.com/
AZURE_VOICELIVE_ENDPOINT=

# Only needed if you are NOT using `--use-token-credential`
AZURE_VOICELIVE_API_KEY=

# Voice Live model + voice (optional)
AZURE_VOICELIVE_MODEL=gpt-realtime
AZURE_VOICELIVE_VOICE=en-US-Ava:DragonHDLatestNeural

# ---------------------------
# App behavior (optional)
# ---------------------------
VOICE_AGENT_TIMEOUT=30
LOG_LEVEL=INFO

# ---------------------------
# Orders backend (optional)
# ---------------------------
# If unset, the app uses an in-memory mock backend.
# If set, the app will call:
#   GET {ORDERS_SERVICE_URL}/orders/{order_id}
#   GET {ORDERS_SERVICE_URL}/orders?customer_name=<name>
ORDERS_SERVICE_URL=http://localhost:8083

