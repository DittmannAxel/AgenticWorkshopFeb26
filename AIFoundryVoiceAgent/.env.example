# =============================================================================
# AIFoundryVoiceAgent - Environment Configuration
# =============================================================================
# Copy this file to .env and fill in your values:
#   cp .env.example .env
# =============================================================================

# Azure Voice Live API endpoint (required)
# Format: wss://<resource-name>.services.ai.azure.com
AZURE_VOICELIVE_ENDPOINT=https://<your-resource>.services.ai.azure.com

# Azure Voice Live API key (required unless using token credential)
AZURE_VOICELIVE_API_KEY=<your-api-key>

# Model deployment name for the realtime model (optional, default: gpt-realtime)
AZURE_VOICELIVE_MODEL=gpt-realtime

# Voice for Text-to-Speech output (optional, default: de-DE-ConradNeural)
# Browse voices: https://learn.microsoft.com/en-us/azure/ai-services/speech-service/language-support?tabs=tts
AZURE_VOICELIVE_VOICE=de-DE-ConradNeural

# Set to "true" to use Azure CLI / Managed Identity instead of API key
# When true, AZURE_VOICELIVE_API_KEY is not required
# Run `az login` locally or use Managed Identity in production
USE_TOKEN_CREDENTIAL=false
